<form class="d-inline" method="post" action="{{ path('delete_zone', {'id': sitePhase.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette zone ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sitePhase.id) }}">
    <button class=" btn btn-danger">Supprimer</button>
</form>