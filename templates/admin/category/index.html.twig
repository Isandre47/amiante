{% extends 'admin/adminBase.html.twig' %}

{% block title %}
    Admin catégories
{% endblock %}

{% block right %}
    Catégories index
    <ul>
        {% if categories is not empty %}
            {% for categorie in categories %}
                <li>{{ categorie.id }} - <a href="{{ path('user_edit',{'id':categorie.id}) }}">Editer</a></li>
            {% endfor %}
        {% else %}
            Aucune catégorie d'enregistrée
        {% endif %}
    </ul>
{% endblock %}